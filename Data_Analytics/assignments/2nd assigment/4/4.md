# Tasks

1. Create a Python script that takes any product name string (e.g., 'Redmi Note 12 Pro') and prints the name in all uppercase and all lowercase using the upper() and lower() methods.

  **answers**
  ```
    product_name = "Redmi Note 12 Pro"
    print(product_name.upper())
    print(product_name.lower())
  ```

2. Write a function clean_brand_name(name) that removes leading/trailing spaces and replaces any hyphens '-' with a single space in the input string. Test it with ' oneplus-Nord '.

  **answers**
  ```
    def clean_brand_name(name):
        return name.strip().replace("-", " ")
    print(clean_brand_name('oneplus-Nord'))
  ```


3. Given the string 'Apple iPhone 14 Pro Max', use string slicing to extract and print only the brand name and the model (i.e., 'Apple' and 'iPhone 14 Pro Max') separately.<br><br><em><strong>Hint:</strong> Use split() to help find the split point, then use slicing for the substrings.</em>

  **answers**
  ```
    product_string = "Apple iPhone 14 Pro Max"
    brand_name = product_string.split()[0]
    model_name = product_string[len(brand_name)+1:]
    print("Brand Name:", brand_name)
    print("Model Name:", model_name)
  ```

4. Build a function format_product_display(name, price) that takes a product name and price (e.g., 'Boat Earbuds', 1299) and returns a formatted string like 'Boat Earbuds - ₹1299'.

  **answers**
  ```
    def format_product_display(name,price):
        return print(name ," - ","₹", price)
    format_product_display('Boat Earbuds', 1299)
  ```

5. Suppose you have a list of messy product names: [' mi-Band 5 ', ' SAMSUNG-Galaxy ', ' realme-Book ']. Write code to clean each name (remove spaces, replace hyphens with spaces, and make the brand title case) and print the cleaned list.<br><br><em><strong>Constraint:</strong> Use at least three string methods from this session.</em>
   
   **answers**
   ```
    messy_product_names = [' mi-Band 5 ', ' SAMSUNG-Galaxy ', ' realme-Book ']
    cleaned_product_names = []

    for name in messy_product_names:
        cleaned_name = name.strip().replace("-", " ").title()
        cleaned_product_names.append(cleaned_name)

    print(cleaned_product_names)
   ```