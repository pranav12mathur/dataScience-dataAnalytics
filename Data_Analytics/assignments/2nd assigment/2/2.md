# Tasks
1. Create three variables in Python: user_name, fav_app, and daily_usage_hours. Assign your own name, your favorite app (like Instagram or Zomato), and how many hours you use it daily.

  **answers**
  ```
    user_name = "Pranav Mathur"
    fav_app = "Instagram"
    daily_usage_hours = 2
  ```

2. Write a Python script that declares variables for product_name, price, and is_available to represent an item on Flipkart. Print each variable and its data type using the type() function.

  **answers**
  ```
    product_name = "PS5 console"
    price = 50000
    is_available = True

    print("Product Name:",product_name)
    print("Data Type of product_name:", type(product_name))
    print("Price:", price)
    print("Data Type of price:", type(price))
    print("Is Available:", is_available)
    print("Data Type of is_available:", type(is_available))
  ``` 

3. Demonstrate the difference between single-line and multi-line comments in Python by writing a script that explains how a Spotify playlist recommendation system might work. Use # for single-line and triple quotes for multi-line comments.

  **answers**
  ```
    # This is a single-line comment explaining the Spotify playlist recommendation system.
    # The system analyzes user listening habits and suggests playlists based on their preferences.

    """
    This is a multi-line comment that provides a more detailed explanation of how the Spotify playlist recommendation system works.
    It uses machine learning algorithms to analyze the user's listening history, favorite genres, and liked songs.
    Based on this data, it generates personalized playlist recommendations that align with the user's musical tastes.
    """
  ```

4. Create variables for order_total, delivery_region, and discount_percent to represent a Zomato order. Follow Python naming conventions and print a sentence using all three variables, like 'Order from [region] totals ₹[order_total] with [discount_percent]% discount.'

  **answers**
  
    order_total = 500
    delivery_region = "Rajkot"
    discount_percent = 10
    print("Order from:", delivery_region,",", "totals ₹:", order_total,",", "with:", discount_percent, "% discount.")

5. Write a Python script that intentionally mixes tabs and spaces for indentation, then fix the script so it runs without errors.<br><br><em><strong>Hint:</strong> Use only spaces for indentation, as per Python's best practices.</em>

  **answers**
  ```
    # Original script with mixed tabs and spaces

    \tprint("Hello, user!")  # This line uses a tab for indentation
        print("Welcome to the app!")  # This line uses spaces for indentation


    # Fixed script using only spaces for indentation

        print("Hello, user!")  # This line uses spaces for indentation
        print("Welcome to the app!")  # This line uses spaces for indentation

    
