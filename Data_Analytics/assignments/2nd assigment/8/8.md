# Tasks
1. Create a Python list called order_amounts with the values [120, 250, 90, 310, 150]. Use a for loop to calculate and print the total order value.

**answers**
  ```
  order_amounts = [120, 250, 90, 310, 150]
  total_order_value = 0
    for amount in order_amounts:
        total_order_value += amount
        
  print("Total order value:", total_order_value)
  ```

2. Given a list of cricket scores [45, 78, 102, 34, 67, 89], use a while loop to print each score until you reach a score above 100, then stop printing.

**answers**
  ```
  cricket_scores = [45, 78, 102, 34, 67, 89]
  i = 0
  while i < len(cricket_scores):
        if cricket_scores[i] > 100:
            break
        print(cricket_scores[i])
        i += 1
  ```
3. Simulate a Flipkart cart with a list of item prices [299, 499, 199, 999, 149]. Use a for loop and the continue statement to skip any item priced below 200, and print the total of the remaining items.

**answers**
  ```
  item_prices = [299, 499, 199, 999, 149]
  total_price = 0
  for price in item_prices:
        if price < 200:
            continue
        total_price += price
        
  print("Total price of items above 200:", total_price)
  ```

4. You have a list of favorite song names: ['Kesariya', 'Believer', 'Shape of You', 'Blinding Lights', 'Excuses']. Use the enumerate() function in a for loop to print each song with its playlist position (starting from 1).

**answers**
  ```
  favorite_songs = ['Kesariya', 'Believer', 'Shape of You', 'Blinding Lights', 'Excuses']
  for index, song in enumerate(favorite_songs, start=0):
        print("Playlist position", index, ":", song)
  ```

5. Write a for loop that goes through a list of Instagram follower counts [120, 1500, 23000, 800, 45000] and prints 'Micro', 'Influencer', or 'Celebrity' for each, based on the following: Micro (<1000), Influencer (1000-10000), Celebrity (>10000).<br><br><em><strong>Hint:</strong> Use if-elif-else inside the loop to check the follower count range.</em>

**answers**
  ```
  follower_counts = [120, 1500, 23000, 800, 45000]
  for count in follower_counts:
        if count < 1000:
            print(count, "is a Micro influencer")
        elif 1000 <= count <= 10000:
            print(count, "is an Influencer")
        else:
            print(count, "is a Celebrity")
  ```